<h1 class="mt-3 ml-4"><i class="fas fa-headphones-alt">Musics</i></h1>
<div class="container">
 <div class="row mt-4">
 <% @musics.each do |music| %>
 <div class="col-5 my-4 offset-1 mb-3">
    <div class="card mt-4 shadow-lg h-100">
      <%= link_to music_path(music) do %>
      <%= attachment_image_tag music, :musicimage,class: "card-img-top"%>
      <% end %>
    <div class="card-body">
        <p class="card-title"><strong><%= music.music_name %></strong></p>
        <p class="card-text　text-wrap"><%= music.music_caption %></p>
        <p class="card-text"><%= music.music_url %></p>
        <span class="card-text">Posted by：<%= music.user.name%></span>&nbsp;<span class="card-text"><%= music.created_at.strftime('%Y/%m/%d') %></span>&nbsp;
        <button type="button" class="btn btn-outline-info"><%= link_to "Detail", music_path(music), class: "text-info"%></button>
        <h5 id="music_favorite_<%= music.id %>">
            <%= render 'favorites/favorite', music: music %>
        </h5>
    </div>
    </div>
  </div>
  <% end %>
 </div>
 <div class="row mt-5">
    <div class="col-2">
      <button class="btn btn-primary">
        <%= link_to :back ,class: "text-light" do%><i class="fas fa-backward">&nbsp;Back</i><% end %>
      </button>
    </div>
    <div class="offset-3 col-5">
    <%= paginate @musics %>
    </div>
 </div>
</div>