<div class="container">
  <h1 class="mt-3 ml-4" id="area-1"><i class="fas fa-music">&nbsp;Group detail</i></h1>
  <div class="row mt-4">
    <div class="col-8 my-4 offset-2">
      <div class="card mt-4 shadow-lg h-70">
        <%= attachment_image_tag @group, :image ,size:"200x200", fallback: "no_image.jpg",class:"mt-3 rounded-circle mx-auto"  %>
        <div class="card-body p-5">
          <h4 class="card-title"><strong><%= @group.name  %></strong></h4>
          <p class="card-text"><%= @group.introduction  %></p>
          <div class="btn"><%= link_to group_group_users_path(@group) do %><i class="fas fa-users fa-lg"><%= @group.users.count %></i><% end %></div>
           <% if @group.owner_id == current_user.id %>
             <div type="button" class="btn "> <%= link_to edit_group_path(@group),class: "text-info" do%><i class="fas fa-edit">Edit</i><% end %></div>
             <div type="button" class="btn "><%= link_to group_path(@group),method: :delete, "data-confirm" => "Do you really delete?",class: "text-danger" do%><i class="fas fa-trash-alt">Delete</i><% end %></div>
           <% end %>
           <% if @group.joined_by?(current_user)  %>
             <div type="button" class="btn text-right"><%= link_to group_group_user_path(@group.id),method: :delete,class: "text-danger" do%><i class="fas fa-walking fa-lg">Leave</i><% end %></div>
           <% else %>
             <div type="button" class="text-right btn"> <%= link_to group_group_users_path(@group.id),method: :post ,class: "text-info" do%><i class="fas fa-house-user fa-lg">Join</i><% end %></div>
           <% end %>

        </div>
      </div>
    </div>
  </div>

  <div class="row yt-5">
    <div class="col-2">
       <button class="btn btn-primary">
         <%= link_to :back ,class: "text-light" do%><i class="fas fa-backward">&nbsp;Back</i><% end %>
       </button>
    </div>
  </div>
</div>



